# 알고리즘 학습 관리

> **운영 규칙**
> 1. 문제는 **약점 아래에만** 둔다. 독립된 "풀 문제 리스트"를 만들지 않는다 — 그게 쌓여서 죽는다.
> 2. 약점 하나당 **최대 3문제.** 넣으려면 하나 빼야 한다.
> 3. 다 풀어서 약점이 해소되면 **그 항목째로 삭제**한다. 완료 목록은 남기지 않는다.
> 4. 약점은 문제 이름이 아니라 **패턴**으로 적는다. 문제는 바뀌어도 패턴은 재사용된다.

---

## W1. 시간복잡도를 손으로 계산하지 않는다 ⭐ 최우선

정답성 테스트만 통과시키고 넘어간다. **오토에버 1번에서 이것 때문에 55% 날림** (로직은 완벽했는데 O(n²)).

**교정법 — 제출 전 두 줄 적기**
```
자료구조가 최대 몇 개까지 커지나?  →  ___
매 스텝 몇 번 순회하나?           →  ___
곱해서 1억 넘으면 구조를 바꿔야 함
```

| 문제 | 확인할 것 |
|---|---|
| 오토에버 1번 재구현 (두 힙으로) | 서버 수 상한 = ? 왜 O(n log n)이 되나 |
| 프로그래머스 42861 섬 연결하기 | 간선을 직접 만들 때 개수 = N²/2. N=1000이면? |
| 프로그래머스 43238 입국심사 | 이분탐색 횟수 × check 비용 = ? |

> **"뽑았다 도로 넣기"가 보이면 구조가 틀린 것.** 오토에버 1번의 정체가 이거였다.

---

## W2. 두 가지 순서가 동시에 필요할 때 자료구조를 못 쪼갠다

"누적 처리량 순"과 "끝나는 시간 순"이 동시에 필요했는데 힙 하나로 우겨넣었다.

| 상황 | 해법 |
|---|---|
| 같은 집합에서 최소·최대 둘 다 꺼냄 | `TreeMap` 하나 (`firstKey` / `lastKey`) |
| 원소가 **상태를 오감** (busy↔free, 대기↔실행) | **상태별로 힙 하나씩**, 시간 흐를 때 이동 |

**골격 (아래 3문제 공통)**
```java
PriorityQueue<long[]> busy = new PriorityQueue<>((a,b) -> Long.compare(a[1], b[1])); // 끝나는 시간
PriorityQueue<Long>   free = new PriorityQueue<>();                                  // 누적/우선순위

for (int i = 0; i < n; i++) {
    while (!busy.isEmpty() && busy.peek()[1] <= i) free.offer(busy.poll()[0]); // 시간 경과분 이동
    long cur = free.isEmpty() ? 0 : free.poll();
    // ... 처리 ...
    busy.offer(new long[]{cur, i + duration});
}
```
각 원소가 힙 사이를 **한 방향으로만** 이동 → 전체 O(n log n).

| 문제 | 왜 |
|---|---|
| LeetCode 1834 Single-Threaded CPU | 입문. 대기/실행 분리의 최소 형태 |
| LeetCode 1882 Process Tasks Using Servers | **오토에버 1번과 사실상 동일** |
| 프로그래머스 42627 디스크 컨트롤러 | 단일 서버 버전 + "취소가 필요한 그리디" |

---

## W3. 경계 조건에서 반복적으로 죽는다

로직은 맞는데 초기값 / 루프 범위 / 정답 위치 숫자 하나로 틀린다.

실제 사례: `visited=1`(→0) · `w=1000`(→L) · 초기화 `i<N`(→좌표 범위) · `dp[N-1]`(→`max(dp)`) · 시작점 방문처리 누락

**제출 전 체크**
```
초기값   : visited=0? dp 초기화 범위? 배열 크기 +1?
루프 범위 : <N 인가 <=N 인가 / 상수 대신 변수(L, N) 썼나
정답 위치 : 마지막 값(dp[L])인가 최댓값(max(dp))인가
자료형   : 곱셈·누적에 long 필요한가
```

| 문제 | 경계 포인트 |
|---|---|
| 프로그래머스 64062 징검다리 건너기 | `consecutive` 초기값 0, `>k` vs `>=k` — **시험 전 유일하게 못 뚫은 것** |
| 프로그래머스 60058 괄호 변환 | u = 앞에서 훑어 `(`,`)` 개수가 **처음 같아지는 지점**에서 자르기 |
| LeetCode 253 Meeting Rooms II | 끝나는 시각 == 시작 시각일 때 겹치나 안 겹치나 |

---

## W4. 제출 전 디버그 프린트를 안 지운다

이것만으로 4~5회 오답 / 효율성 실패. 문제 없음, **습관만.**

> **제출 전 `Ctrl+F` → `println` 검색.** 3초.

---

## 미학습 유형 (약점 아니라 공백)

| 유형 | 대표 문제 |
|---|---|
| 플로이드-워셜 (모든 쌍 최단경로) | 프로그래머스 72413 합승 택시 요금 |
| 상태 BFS (`visited[r][c][방향]`) | 프로그래머스 67259 경주로 건설 |
| 백트래킹 응용 (조합 + 중복 제거) | 프로그래머스 64064 불량 사용자 |

---

## 보유 무기

TSP(비트마스크 DP) · 힙/PQ · BFS · DFS/백트래킹 · MST+유니온파인드(경로압축)
· DP(배낭·LIS·격자) · 파라메트릭 이분탐색 · 다익스트라 · 그리디
