# 알고리즘 학습 관리

> **운영 규칙**
> 1. 문제는 **약점 아래에만** 둔다. 독립된 "풀 문제 리스트"를 만들지 않는다 — 그게 쌓여서 죽는다.
> 2. 약점 하나당 **최대 3문제.** 넣으려면 하나 빼야 한다.
> 3. 다 풀어서 약점이 해소되면 **그 항목째로 삭제**한다. 완료 목록은 남기지 않는다.
> 4. 약점은 문제 이름이 아니라 **패턴**으로 적는다.

---

## W1. 루프 안에서 "찾고" 있으면, 밖에서 미리 만들어라 ⭐ 최우선

**오토에버 두 문제를 모두 이걸로 날렸다.** 유형 판정도 맞았고 정답성도 완벽했는데, 안쪽 루프 비용을 안 세서 시간초과.

| | 무엇을 했나 | 왜 죽었나 | 정석 |
|---|---|---|---|
| **1번** | 가용 서버 찾을 때까지 힙을 **뽑았다 도로 넣기** | 매 스텝 O(서버 수) → **O(n²)** | **힙 2개**(busy/free)로 분리, 시간 흐를 때 한 방향 이동 |
| **2번** | 필수쌍마다 **인접리스트를 정렬 + 스캔** | 같은 리스트를 5만 번 정렬 → 차수 편중 시 **10¹⁰** | **HashMap에 쌍별 min/max 미리 집계** → O(1) 조회 |

> 2번은 "정렬해두고 `break`로 빨리 찾기"를 시도했다. 정답은 유지되지만 **스캔 비용을 정렬 비용으로 바꾼 것뿐**이었다 (203초 → 106초, 제한의 50배).
> **같은 리스트를 매번 정렬하고 있으면 = 루프 밖에서 한 번만 하면 되는 일이다.**

**실측 (n=50,000, m=200,000)**
```
1번  두 힙  16ms   vs  내 코드  50분+
2번  해시맵 98ms   vs  내 코드  203초   ← 차수 편중 데이터
```

### 체감 훈련 (설명 말고 숫자로)
> 다익스트라 때 `visitCount` 심어서 13 → 6 찍히는 걸 보고 이해했다. **같은 방법을 쓴다.**

1. 내 코드와 정석 코드를 **둘 다** 짠다
2. 안쪽 루프에 카운터를 심는다 (`cnt++`)
3. n을 100 → 1,000 → 10,000으로 키우며 **카운터 값을 표로 적는다**
4. 내 코드가 n²로 늘어나는 걸 눈으로 본다

### 제출 전 두 줄 (기계적으로)
```
바깥 루프 횟수  ×  안쪽에서 만지는 원소 수  =  ?
1억 넘으면 구조를 바꿔야 함
```
> 감각을 기르려 하지 말고, **일단 기계적으로 센다.** 세다 보면 감각이 붙는다.

### 자료구조 접근 비용 (이게 근본)
| 연산 | 비용 | 함정 |
|---|---|---|
| `HashMap.get/put` | **O(1)** | 루프 안 탐색은 거의 다 이걸로 대체 가능 |
| `ArrayList.contains` / 전체 순회 | **O(n)** | ⚠️ 루프 안에 있으면 즉시 O(n²) |
| `PriorityQueue.offer/poll` | O(log n) | `peek`은 O(1) |
| `PriorityQueue` 안에서 특정 원소 찾기 | **O(n)** | ⚠️ 힙은 "탐색"용이 아님 — 이게 1번의 정체 |
| `TreeMap.firstKey/lastKey` | O(log n) | 최소·최대 동시에 필요할 때 |
| `Collections.sort` | O(n log n) | ⚠️ 루프 안에 있으면 즉시 폭발 |

**한 문장:** *"힙과 리스트는 꺼내는 도구지 찾는 도구가 아니다. 찾을 거면 HashMap."*

| 문제 | 왜 |
|---|---|
| 오토에버 2번 재구현 (HashMap 버전) | 인접리스트 스캔 → O(1) 조회. **W1의 마지막 조각** |

> **두 힙 파트 졸업** (2026-07-29): 디스크 컨트롤러 → 오토에버 1번 재구현(8ms) → LeetCode 1834(스스로 50분) → 1882(208ms, 상위 89%).
> 세 문제 모두 두 힙 구조를 스스로 세웠고, 틀린 건 매번 **문제 고유의 시간 규칙**이었다.

**두 힙 골격**
```java
PriorityQueue<long[]> busy = new PriorityQueue<>((a,b) -> Long.compare(a[1], b[1])); // 끝나는 시간
PriorityQueue<Long>   free = new PriorityQueue<>();                                  // 누적/우선순위

for (int i = 0; i < n; i++) {
    while (!busy.isEmpty() && busy.peek()[1] <= i) free.offer(busy.poll()[0]); // 경과분 이동
    long cur = free.isEmpty() ? 0 : free.poll();
    // ... 처리 ...
    busy.offer(new long[]{cur, i + duration});
}
```
각 원소가 힙 사이를 **한 방향으로만** 이동 → 전체 O(n log n).
> "뽑았다 도로 넣기"가 보이면 구조가 틀린 것.

---

### 참조용 도구 (외우지 말 것 — 막혔을 때 열어볼 것)

**① 시간 규칙 3질문** — 두 힙 계열에서 매번 틀리는 건 구조가 아니라 이 셋이다.
```
1. 시각은 어떻게 흐르나?     (1씩? 이벤트마다 점프?)
2. 한 시각에 몇 개 처리되나?  (1개만? 여러 개 동시 가능?)
3. 언제부터 처리 가능한가?    (도착 시각 제약)
```
> 실제 사례: 1882에서 2번을 안 물어서 `curTime++`로 틀렸다. 정답은 `curTime = max(curTime, idx)`.
> **시간이 흐르는 이유는 "기다릴 때"뿐. 할당은 시간 0.**

**② 긴 반례를 짧게 만들기** — 리트코드가 준 테케가 500칸이라 손으로 못 볼 때.
```
1. 느려도 확실한 브루트포스를 만든다
2. 일부러 작은 랜덤 입력(원소 3~6개)을 수만 개 돌려 비교
3. 첫 불일치 = 손으로 추적 가능한 최소 반례
```

---

## W2. 경계 조건에서 반복적으로 죽는다

로직은 맞는데 초기값 / 루프 범위 / 정답 위치 숫자 하나로 틀린다.

실제 사례: `visited=1`(→0) · `w=1000`(→L) · 초기화 `i<N`(→좌표 범위) · `dp[N-1]`(→`max(dp)`) · 시작점 방문처리 누락

**제출 전 체크**
```
초기값   : visited=0? dp 초기화 범위? 배열 크기 +1?
루프 범위 : <N 인가 <=N 인가 / 상수 대신 변수(L, N) 썼나
정답 위치 : 마지막 값(dp[L])인가 최댓값(max(dp))인가
자료형   : 곱셈·누적에 long 필요한가
```

| 문제 | 경계 포인트 |
|---|---|
| 프로그래머스 64062 징검다리 건너기 | `consecutive` 초기값 0, `>k` vs `>=k` — **아직 못 뚫음** |
| 프로그래머스 60058 괄호 변환 | u = 앞에서 훑어 `(`,`)` 개수가 **처음 같아지는 지점**에서 자르기 |
| LeetCode 253 Meeting Rooms II | 끝나는 시각 == 시작 시각일 때 겹치나 |

---

## W3. 제출 전 디버그 프린트를 안 지운다

이것만으로 4~5회 오답 / 효율성 실패. 문제 없음, **습관만.**

> **제출 전 `Ctrl+F` → `println` 검색.** 3초.

---

## 미학습 유형 (약점 아니라 공백)

| 유형 | 대표 문제 |
|---|---|
| 플로이드-워셜 (모든 쌍 최단경로) | 프로그래머스 72413 합승 택시 요금 |
| 상태 BFS (`visited[r][c][방향]`) | 프로그래머스 67259 경주로 건설 |
| 백트래킹 응용 (조합 + 중복 제거) | 프로그래머스 64064 불량 사용자 |

---

## 보유 무기

TSP(비트마스크 DP) · **두 힙(스케줄링)** · 힙/PQ · BFS · DFS/백트래킹 · MST+유니온파인드(경로압축)
· DP(배낭·LIS·격자) · 파라메트릭 이분탐색 · 다익스트라 · 그리디

**유형 판정은 이미 된다** — 오토에버 두 문제 모두 유형(힙 스케줄링 / MST+필수간선)을 정확히 잡았다.
막힌 건 유형이 아니라 **W1**이다.
